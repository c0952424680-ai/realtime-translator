
const LiveEvents={timer:null,start(){this.refresh();clearInterval(this.timer);this.timer=setInterval(()=>this.refresh(),180000);window.addEventListener("state-changed",e=>{if(["manual","gps-ok","reverse-ok","nearby-gps"].includes(e.detail.reason))this.refresh()});document.getElementById("refreshLive")?.addEventListener("click",()=>this.refresh())},async getJson(url,ms=5000){const c=new AbortController(),t=setTimeout(()=>c.abort(),ms);try{const r=await fetch(url,{cache:"no-store",signal:c.signal});if(!r.ok)throw new Error("http");return await r.json()}finally{clearTimeout(t)}},async weather(){const s=StateCore.get();try{const j=await this.getJson(`https://api.open-meteo.com/v1/forecast?latitude=${s.lat}&longitude=${s.lon}&current=temperature_2m,apparent_temperature,precipitation,weather_code,wind_speed_10m&timezone=auto`,5000),c=j.current||{};RiskEngine.weather={temp:c.temperature_2m,feel:c.apparent_temperature,rain:c.precipitation,wind:c.wind_speed_10m,code:c.weather_code,time:c.time};RiskEngine.sources.weather="ok";App.health("weather","ok","Open-Meteo");this.renderWeather()}catch{RiskEngine.sources.weather="error";App.health("weather","error","Open-Meteo 無回應");this.renderWeather(true)}},async quake(){const s=StateCore.get();try{const j=await this.getJson("https://earthquake.usgs.gov/earthquakes/feed/v1.0/summary/all_day.geojson",5500),arr=[];for(const f of j.features||[]){const p=f.properties||{},co=f.geometry?.coordinates||[],mag=Number(p.mag),lat=Number(co[1]),lon=Number(co[0]);if(!Number.isFinite(mag)||mag<3.5)continue;const d=App.distanceKm(s.lat,s.lon,lat,lon);if(d>1200)continue;const sev=(mag>=6.5&&d<=250)?"red":((mag>=5.5&&d<=500)||(mag>=4&&d<=80))?"orange":"green";arr.push({title:`M${mag.toFixed(1)} ${p.place||"地震"}`,summary:`距離約 ${Math.round(d)} 公里`,sev,url:p.url})}RiskEngine.quakes=arr.slice(0,10);RiskEngine.sources.quake="ok";App.health("quake","ok",`USGS ${arr.length} 筆`)}catch{RiskEngine.sources.quake="error";App.health("quake","error","USGS 無回應")}},async refresh(){this.setStatus("背景更新中…");NewsEngine.items=[];await Promise.allSettled([this.weather(),this.quake(),NewsEngine.gdacs()]);RiskEngine.sources.news=NewsEngine.items.length?"ok":"idle";RiskEngine.evaluate();this.renderNews();App.renderHealth();this.setStatus(`更新完成 ${new Date().toLocaleTimeString("zh-TW",{hour:"2-digit",minute:"2-digit"})}`)},renderWeather(fail=false){const e=document.getElementById("weatherBox");if(!e)return;if(!RiskEngine.weather){e.innerHTML=`<div class="status">${fail?"天氣暫時無法取得":"讀取天氣中…"}</div>`;return}const w=RiskEngine.weather;e.innerHTML=`<div class="kpis"><div class="kpi"><span>氣溫</span><b>${w.temp}°C</b></div><div class="kpi"><span>體感</span><b>${w.feel}°C</b></div><div class="kpi"><span>降雨</span><b>${w.rain} mm</b></div><div class="kpi"><span>風速</span><b>${w.wind} km/h</b></div></div><div class="muted">Open-Meteo｜${w.time||""}</div>`},renderNews(){const e=document.getElementById("newsBox");if(!e)return;const rows=NewsEngine.merge();e.innerHTML=rows.length?rows.map(x=>`<div class="news-item"><a href="${x.url}" target="_blank" rel="noopener">${esc(x.title)}</a><small>${esc(x.source||"")}</small></div>`).join(""):`<div class="status">目前沒有可顯示的重大事件資料。</div>`},setStatus(t){document.querySelectorAll("[data-live-status]").forEach(e=>e.textContent=t)}};
window.LiveEvents=LiveEvents;
